name: eternal_match
description: A new Flutter project for EternalMatch
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.14.0
  firebase_analytics: ^10.3.0

  # HTTP package بديل Alova
  http: ^1.2.0

  # Agora (Audio/Video calls)
  agora_rtc_engine: ^6.2.0

  # Permissions for camera and microphone
  permission_handler: ^10.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true